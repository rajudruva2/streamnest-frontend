apiVersion: v1
kind: Service
metadata:
  name: streamnest-frontend
  namespace: streamnest

spec:
  selector:
    app: streamnest-frontend

  ports:
    - port: 80
      targetPort: 80

  type: ClusterIP
